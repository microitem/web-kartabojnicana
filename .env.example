# ─── App links (W5) ──────────────────────────────────────────────────────────
# Base URL of the application every CTA on this site links into. Not a secret —
# only present so dev/staging can point at a different app instance if needed.
# NEXT_PUBLIC_APP_URL=https://app.kartabojnicana.sk
